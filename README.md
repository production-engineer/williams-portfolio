# Williams Portfolio

Personal portfolio site for Erik Williams. Showcases projects, a photo gallery, resume download, and a contact form.

## Stack

Single-file static HTML/CSS/JS — no build step, no dependencies.

## Deployment

Deploys automatically to GitHub Pages on push to `main` via `.github/workflows/deploy.yml`.

## Development

Open `index.html` directly in a browser — no server required.
